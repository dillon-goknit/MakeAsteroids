[project]
name = "makeasteroids"
version = "0.1.0"
description = "Pygame Asteroids-style game for practicing OOP"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame==2.6.1",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
